services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      DATABASE_URL: file:/data/dev.db
      NODE_ENV: production
    volumes:
      - cancorbs-sqlite:/data

volumes:
  cancorbs-sqlite:
  caddy_data:
  caddy_config:
